package com.yg.blog.utils;
/*
import org.hibernate.collection.spi.PersistentBag;
import org.modelmapper.AbstractConverter;
import org.springframework.stereotype.Component;

import com.yg.blog.payloads.CommentDto; // Import the correct CommentDto

import java.util.List;
import java.util.stream.Collectors;

@Component
public class PersistentBagToListConverter extends AbstractConverter<PersistentBag<CommentDto>, List<CommentDto>> {
    @Override
    protected List<CommentDto> convert(PersistentBag<CommentDto> source) {
        return source.stream().collect(Collectors.toList());
    }
}

*/